# ================================================================================
# INDECIMAL RAG CHATBOT - PYTHON DEPENDENCIES
# ================================================================================

# Core RAG dependencies
sentence-transformers>=2.2.0  # Text embeddings (all-MiniLM-L6-v2)
faiss-cpu>=1.7.4              # Vector similarity search

# LLM backends
ollama>=0.1.0                 # Local LLM (primary - for local use)
openai>=1.0.0                 # OpenRouter API client (fallback - for cloud deployment)

# Web UI
streamlit>=1.28.0             # Chat interface framework

# Utilities
numpy>=1.24.0                 # Numerical operations
